// TEMPORAIREMENT DÉSACTIVÉ - Service salon booking
// Le fichier original a été renommé en salonBookingService.js.disabled

/**
 * Stub function - disabled temporarily
 */
export const createSalonBooking = async (bookingData) => {
    console.warn('salonBookingService is temporarily disabled');
    return null;
};

/**
 * Stub function - disabled temporarily
 */
export const getClientBookings = async (clientId) => {
    console.warn('salonBookingService is temporarily disabled');
    return [];
};

/**
 * Stub function - disabled temporarily
 */
export const getSalonBookingsForDate = async (salonId, date) => {
    console.warn('salonBookingService is temporarily disabled');
    return [];
};

/**
 * Stub function - disabled temporarily
 */
export const updateBookingStatus = async (bookingId, status) => {
    console.warn('salonBookingService is temporarily disabled');
    return null;
};

/**
 * Stub function - disabled temporarily
 */
export const cancelBooking = async (bookingId) => {
    console.warn('salonBookingService is temporarily disabled');
    return null;
};
