{
  "name": "union-digitale-tests",
  "version": "1.0.0",
  "description": "Tests E2E pour Union Digitale",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:lighthouse": "playwright test --grep @lighthouse",
    "test:buyer": "playwright test --grep 'Flow Acheteur'",
    "test:seller": "playwright test --grep 'Flow Vendeur'",
    "test:moncash": "playwright test --grep 'MonCash'"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "playwright-lighthouse": "^3.2.0"
  }
}